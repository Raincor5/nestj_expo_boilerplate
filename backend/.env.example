# Backend environment example
# Prefer the Neon connection string (includes sslmode=require)
DATABASE_URL=postgresql://USER:PASSWORD@ep-neon-id.aws.neon.tech/neondb?sslmode=require
DB_SSL=true

# Local fallback (ignored when DATABASE_URL is present)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=auth_db

JWT_SECRET=change_me
JWT_REFRESH_SECRET=change_me
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

PORT=3000
NODE_ENV=development
